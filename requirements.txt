requests==2.31.0
PyYAML==6.0.1
apscheduler==3.10.1
openapi-spec-validator==0.6.0
fastapi==0.104.1
uvicorn==0.23.2
python-dotenv==1.0.0

# Note: oasdiff must be installed separately as a system binary
# See: https://github.com/oasdiff/oasdiff#installation
# For Go users: go install github.com/oasdiff/oasdiff@latest
# For macOS users: brew tap oasdiff/homebrew-oasdiff && brew install oasdiff 